import {ref} from 'vue'
export const dataIsLoading = ref(true)
///考试id和试卷id和考试记录id
export const examId = ref(-1)

// 试卷ID
export const paperId = ref(-1)

// 作答记录ID
export const recordId = ref(-1)

// 题目
export const questions = ref()